{
    "name": "any-player-ui",
    "version": "0.1.0",
    "description": "Desktop UI for Any Player",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "tsc --watch",
        "build": "tsc && npm run copy-assets",
        "copy-assets": "bash build.sh",
        "clean": "rm -rf dist/**/*.js dist/**/*.js.map dist/**/*.d.ts"
    },
    "devDependencies": {
        "@tauri-apps/api": "^1.5.0",
        "typescript": "^5.3.3"
    }
}